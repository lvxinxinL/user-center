spring:
  applicaiton:
    name: user-center
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yupi
    username: root
    password: root@csu
# 应用服务 WEB 访问端口
server:
  port: 8080

